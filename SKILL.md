---
name: xhs-douyin-engagement-snapshot
description: "增量采集小红书和抖音的点赞/收藏内容，按稳定内容 ID 去重并持久化快照；对用户指定或本次新发现的候选进入详情/视频页做只读深读。适用于定时任务、收藏/点赞变化监测、内容选题素材池和跨平台去重分析。"
---

# 小红书与抖音点赞/收藏增量采集

## 目标与边界

本技能的默认目标是：从两个平台现有的点赞/喜欢和收藏列表中，持续发现此前未采集过的内容，并留下可追溯的原始快照、去重结果和覆盖边界。

默认只读。禁止自动点赞、取消点赞、收藏、取消收藏、关注、评论、私信、发布、购买、导出账号数据或修改飞书 Base。进入详情页或视频页只用于读取公开可见信息；如果页面出现登录、验证码、权限、付费或需要互动才能继续的状态，停止并记录边界。

## 自然语言驱动

用户不需要记住技能名或参数。以下中文表达都视为调用本技能：

- “执行一次”“按上次流程看看小红书和抖音的点赞收藏”“采集新增内容”。
- “把新增内容分析成选题”“整理成待定选题”。主报告先交付少量建议选题，机制卡作为证据附录；“整理成待定选题”先生成建议选题和写入预览，只有用户确认后才写飞书。
- “看看这几条详情”“把这几个视频深读一下”。只对当前运行已读到的内容或用户明确提供的内容进入详情/视频页。

自然语言默认值：小红书和抖音都执行；小红书读取点赞/收藏，抖音读取喜欢/收藏夹-视频；每个列表前 5 屏增量扫描；只读；不写飞书；不进入具体内容详情。用户明确追加“详情/视频/深读”或提供候选后，再启用深读；用户明确提供 Base 并确认写入后，再启用飞书写入。

## 每次执行前的源同步检查（强制）

本技能的唯一维护源是 GitHub 仓库：

`https://github.com/haoqiguai-dizao/xhs-douyin-engagement-snapshot`

默认跟踪 `main` 分支。每次执行任何浏览器操作、读取平台列表或生成报告之前，必须完成以下前置检查：

1. 通过 GitHub 连接器读取 `main` 的当前提交或 `SKILL.md`、`INSTALL.md`、`scripts/` 文件内容；如果本机是该仓库的 Git checkout，也可先执行 `git fetch` 和 `git pull --ff-only`。
2. 将 GitHub 版本与当前加载的技能副本比较。发现差异时，先同步完整技能目录，再重新读取并遵守更新后的 `SKILL.md`；不能用旧规则继续执行本次任务。
3. 同步应先写入临时位置并完成文件完整性检查，再替换技能副本；不得覆盖运行数据、登录信息、Cookie、飞书配置或 `数据/平台互动快照/`。
4. 记录本次使用的仓库提交 SHA、同步时间和是否发生更新，并在报告中注明。
5. GitHub 读取、网络或权限检查失败时，停止本次平台操作并报告“源同步失败”；只有用户在同一请求中明确允许离线使用指定版本时，才能例外继续。

这条检查优先于“复用已有浏览器标签页”以及所有采集、深读、分析和飞书写入步骤。安装包也必须从已确认的 `main` 版本生成，不能把旧的桌面压缩包当作更新源。

## 输入参数

定时任务或手动运行时，按以下参数理解用户意图；没有给出时使用默认值：

- `platforms`: `xhs,douyin`。
- `lists`: 小红书 `liked,favorited`；抖音 `liked,favorited_videos`。
- `coverage_mode`: `incremental_bounded`。从列表顶部开始，默认扫描前几屏，不做无界全量扫描。
- `deep_read`: `false`。只对用户指定的 `deep_read_ids` 或本次新发现且满足明确规则的少量候选深读。
- `deep_read_ids`: 空。只接受本次已从页面 DOM 读到的稳定 ID 或页面链接，不接受猜测的深链。
- `deep_read_limit`: 10；超过上限必须停止并报告未深读数量。
- `analysis_mode`: `content_mechanism`；默认提炼角度、设定、矛盾、承诺、句式、措辞和可迁移模板，不接受仅按关键词命名的主题簇。
- `business_lens`: `merchant_community`；默认优先判断内容机制能否服务门店、社群、客户经营和内容增长，但这只是候选筛选与正文落点，不要求标题反复出现“门店/社群/客户”等行业词。
- `title_strategy`: `attention_first`；建议标题优先使用采集证据中真实出现的反差、冲突、失败现场、数字、时间、价格、身份代入、反常识或信息缺口，再把业务价值写入辅助字段。
- `source_repo`: `https://github.com/haoqiguai-dizao/xhs-douyin-engagement-snapshot`；固定技能源，不从其他仓库或本地旧副本推断更新。
- `source_branch`: `main`；每次执行前强制检查。
- `max_screens_per_list`: 5；默认每个平台每个列表只扫描前 5 屏。
- `max_scrolls_per_list`: 8；达到上限时必须将 `coverage_status` 标记为 `capped`，不能声称已采全。
- `known_boundary_pages`: 2。连续两屏都只出现历史已知 ID 时提前停止；这只是增量边界，不是全量证明。
- `feishu_write`: `false`。只有用户已经确认本技能的 Base 写入方案，或定时任务显式带入已授权配置时才设为 `true`。
- `feishu_write_mode`: `pending_only`。只新增“人工决定=待定”的候选方向，不更新或覆盖已有人工决定。
- `feishu_target`: 运行时提供的 Base URL、表名/表 ID、可选视图名和字段映射；技能包不写死任何 Base Token、Table ID、View ID 或字段 ID。

## 浏览器前置与标签页

1. 使用内置浏览器的语义 DOM 能力。不要使用坐标点击、脚本注入、猜测 URL 或直接输入点赞/收藏深链来冒充交互成功。
2. 开始前确认并复用飞书 Base 入口：当前项目从项目 `AGENTS.md` 读取；跨设备或其他项目必须由 `feishu_target.base_url` 提供。已存在则复用，不重复打开；始终保留该标签页。没有目标 URL 时只做本地快照和报告，不猜测或写入任意 Base。
3. 小红书按“首页 → 我 → 收藏/点赞”语义导航；抖音按“精选 → 我的 → 收藏 → 收藏夹 → 视频”或“喜欢”语义导航。每次点击前重新读取可见 DOM，点击后重新读取可见状态和 URL。
4. 任务结束保留飞书、小红书、抖音标签页；不把临时标签页留在外面。

## 增量采集流程

对每个平台的每个列表单独执行以下流程：

1. **建立运行边界**：记录 `run_id`、`run_started_at`、平台、列表类型、入口 URL 和页面状态。
2. **读取当前视窗**：读取语义 DOM/可访问快照，提取所有内容卡片的标题、作者、可见互动数、内容链接、公开可见时间字段和稳定内容 ID。截图作为可选证据。
3. **增量判断**：用 `(platform, list_type, content_id)` 与历史索引比较。首次出现写入 `is_new=true`；已存在写入 `is_new=false` 并更新 `last_seen_at`。同一内容同时出现在点赞和收藏列表时保留两条列表关系，不合并成一条。
4. **继续加载**：使用语义滚动或页面提供的“加载更多”，每次滚动后重新读取 DOM，直到达到前 `max_screens_per_list` 屏、遇到已知边界或触发安全上限。不得假定一次 DOM 快照就是全量，也不做无界滚动。
5. **停止条件**：
   - 连续 `known_boundary_pages` 屏没有新 ID；或
   - 达到 `max_screens_per_list`；或
   - 到达明确的列表末端/空状态/加载结束标记；或
   - 达到 `max_scrolls_per_list`，此时状态为 `capped`；或
   - 页面发生登录、验证码、权限、脚本/路由超时等阻塞，状态为 `blocked`。
6. **覆盖声明**：输出 `coverage_status` 为 `bounded_top_screens`、`incremental_boundary`、`complete_end_reached`、`capped`、`blocked` 之一，并记录屏数、滚动次数、读取页数和新 ID 数量。`bounded_top_screens` 明确表示只覆盖前几屏，不能回答全量问题。

### 重要的排序与删除规则

- 不把列表顺序当成真实点赞/收藏时间。
- 内容从当前列表消失不自动解释为取消点赞/收藏；可能是排序、分页、权限、平台展示变化或读取未到达该位置。
- 只有在覆盖边界可比、连续多次观察到消失且没有页面异常时，才将其标记为“疑似移出”，仍需人工确认。
- 内部 URL ID 可用于去重，不可从中推导用户的操作时间。

## 深度读取流程

深读不是默认动作。只有 `deep_read=true` 且内容 ID 已从当前列表 DOM 读到，或用户明确指定了这些 ID，才执行：

1. 从当前列表的语义链接进入详情页/视频页，不猜测或手写深链。
2. 读取并保存可见的标题、作者、正文、标签、公开发布时间（若存在）、媒体类型、字幕/可见转写、互动数和页面 URL。
3. 视频只读取页面已经提供的可见标题、字幕、文本或可访问描述；不自动点赞、收藏、关注、评论、下载、转发或发布。若必须播放、登录或互动才能得到信息，记录 `deep_read_status=blocked`。
4. 每次深读保存截图/证据引用（若浏览器接口能提供），并回到原列表页继续采集。
5. 深读结果与原始列表记录分开保存。不得把一条深读推断当成平台整体趋势；内容方向仍需进入人工筛选和 B 选题点击测试流程。

## 采集后的内容机制分析

每次运行都要基于本次新增内容和必要的历史对照生成候选方向，但输出必须从“关键词聚类”升级为“内容机制分析”。只写“社群/私域/亲子/AI”等主题词，不算完成。

### 候选形成顺序与计权

选题必须从采集证据中形成，不能先写业务标题再回头寻找支持链接：

1. **先还原独立内容**：列表关系继续按 `(platform, list_type, content_id)` 保存；形成选题时先按 `(platform, content_id)` 合并为独立内容。同一内容同时出现在点赞和收藏中可以作为关系强度信号，但不能算成两条独立证据。
2. **先形成证据组**：根据原始标题中的具体问题、对象、场景、冲突、结果承诺和表达机制，把至少 2 条独立内容组成证据组；不得先给证据组命名为一个预设业务答案。
3. **再判断业务相关性**：使用 `business_lens` 判断该证据组是否值得服务门店社群，但不得为了适配业务而改写原始内容事实，或给没有证据的内容补上门店、社群、福利、成交等设定。
4. **最后生成注意力标题**：标题从证据组共同成立的注意力机制出发；门店社群的使用场景、经营问题和正文落点写入标题后的辅助字段。
5. **反向核验**：生成后逐条检查标题中的对象、数字、冲突和承诺能否回指原始证据。不能回指的部分标为工作推断；如果核心钩子不能回指，则放弃该标题。

必须避免确认偏差：不得因为已经想写“门店群没人回应”，就从大量素材中只挑能靠近这一结论的内容。证据组必须在候选标题出现前形成并记录。

### 两层证据

1. **标题层**：对每条去重内容保留原始标题，并提取 `角度`、`具体设定`、`目标人群`、`经营矛盾/冲突`、`利益或结果承诺`、`标题句式`、`措辞动作`、`数字/时间/价格表达`、`情绪与悬念装置`、`证据形态`。可使用 `scripts/analyze_titles.py` 生成规则化特征，但规则特征只能辅助，不能替代人工/模型语义判断。
2. **详情/视频层**：只有用户明确要求深读，或候选因标题信息不足而进入 `deep_read` 时，才读取正文、首屏、字幕/可见转写、案例顺序、镜头或演示方式。详情事实与标题事实分开存储，不能用标题推断正文效果。

### 每条内容的最小机制卡

```text
内容证据：平台、列表、content_id、原始标题、链接、coverage_status
角度：从什么切口讲，而不是讲什么大类
设定：谁在什么具体场景、身份或时间压力下遇到什么事
经营矛盾：表面问题与真正阻力
承诺：结果、时间、数量、价格、风险规避或情绪体验
句式骨架：例如“失败场景提问→五步补救”
措辞动作：警告、否定重构、悬念延迟、口语代入、身份背书、价格锚定等
可迁移模板：用占位符改写成适合目标业务的标题骨架
证据边界：标题可见 / 详情已读 / 视频字幕已读 / 尚未验证
```

### 候选方向的最低交付标准

每个方向必须同时包含：

1. **注意力优先的标题性质文本**：标题中能读出具体反差、冲突、失败现场、结果、数字、时间压力、身份代入、反常识或信息缺口，而不是一个主题名或行业说明；完整业务解释放在标题后的辅助字段。
2. **至少 2 条独立原始证据**：列出平台、列表、content_id、原始标题和链接；同一 `(platform, content_id)` 的点赞与收藏关系只能算一条内容证据。单条独立内容只能作为灵感，不得升级成方向。
3. **机制解释**：说明这些内容共同使用了什么角度、设定、句式或措辞，以及哪些只是表面相似。
4. **可迁移改写**：给出 1 个通用句式和 2 个面向当前业务的示例标题；示例必须标注为工作推断，不伪装成原文。
5. **深读建议**：说明仅看标题还缺什么，是否需要进入详情/视频，以及建议深读的具体 content_id。
6. **事实/推断边界**：明确样本覆盖、未读取的正文/视频信息、没有验证的表现因果；不得自动选 TOP。

### 最终交付：建议标题优先

机制卡是分析过程中的证据，不是用户最终要接收的结果。每次运行的人读报告必须先给出 3—5 条（或证据不足时明确少于 3 条）**建议标题/标题性质的选题**，机制卡与原始标题放在后面的证据附录。

这里的“建议标题”不是抽象的业务命题，也不是“社群运营方向”这类主题名，而是已经带有具体对象、场景、冲突、承诺或悬念的可读文本，可以直接进入人工筛选、标题改写和 B 阶段点击测试。

`business_lens=merchant_community` 表示选题最终要对门店社群有用，不表示每个标题都必须出现“门店、社群、客户、经营者”。行业词只有在它本身能增强身份代入或界定必要场景时才进入标题；否则放在“适用场景/人群”“业务落点”和正文设定中。标题先负责让目标读者停下来，辅助字段再说明为什么值得门店社群采用。

注意力抓取必须来自可兑现的信息，不等于堆“震惊、千万别、后悔就晚了”等空泛刺激。优先使用：

- **强反差**：相同对象在价格、结果、身份或行为上的显著差异；
- **失败现场**：越努力却越无效、动作与结果相反的具体场景；
- **数字/时间/价格锚点**：前提是采集标题或详情中确有对应证据；
- **否定重构/反常识**：指出惯性做法为什么失效，而不是无证据唱反调；
- **信息缺口**：让读者知道问题是什么，同时保留值得点开的关键原因；
- **窄身份代入**：只有当角色能显著增强相关性时才使用店长、老板、小团队等身份。

每条建议标题必须包含：

```text
建议标题：标题性质的可读文本，不是主题词或抽象命题
注意力钩子：反差、冲突、失败现场、数字/时间/价格、反常识或信息缺口；注明对应证据
选题意图：这条标题准备切哪个问题/角度
适用场景/人群：具体角色、时机或使用场景；门店社群可以写在这里，不强塞进标题
用户/经营矛盾：为什么现在会遇到问题，表面问题和真正阻力分别是什么
建议角度与设定：准备从哪个切口、以什么身份/现场/冲突来讲
业务落点：该注意力母题如何服务门店社群，以及正文准备落到什么经营动作
内容承诺：看完能得到什么结果、判断、步骤或情绪体验
表达拆解：句式、关键措辞、数字/时间/价格/情绪装置
证据：至少 2 条原始内容及链接；标注标题层或详情/视频层
下一步：是否需要深读；若需要，列出 content_id；否则进入人工 B 阶段筛选
状态：待人工复核，未进入 B
```

建议标题不是把原始标题换几个词，也不是把多个关键词拼起来。它必须把观察到的内容机制翻译成当前业务里可以继续改写和测试的标题文本。可以给出“建议先验证顺序”，但不得称为平台 TOP、爆款或自动入选。

当本次输出 3 条及以上建议标题时，至少使用 3 种不同的注意力机制；除非原始证据本身高度集中，否则不得让全部标题都从“门店/社群/客户”身份词切入，也不得让超过一半标题重复使用同一个行业词或同一种问句骨架。业务方向可以一致，表面入口必须保持真实、多样且由证据驱动。

示例：

- 弱：`门店社群如何提升客户互动？`（业务正确，但没有具体现场和注意力张力）
- 强：`为什么你越认真发通知，群里越没人说话？`（失败现场和结果反转；门店社群落点放在辅助字段）
- 弱：`门店如何塑造商品价值？`（抽象经营命题）
- 强：`同样一张纸，为什么有人卖 2 块，有人敢卖 100？`（价格反差；正文再迁移到福利和价值表达）

如果样本只能支持机制观察、不能形成标题性质的文本，最终交付必须写“本次证据不足，暂不形成建议标题”，并说明缺少什么深读或业务信息；不得为了凑数量生成泛方向。

### 推荐的机制标签

标签不是最终选题，只是分析维度，可组合使用：

- `风险警告/避坑`、`失败场景提问/补救`、`否定常识/重定义`；
- `明确结果+时间承诺`、`数字清单/模板可复制`、`身份/经验背书`；
- `具体地点+成本利益`、`现场叙事/第一视角`、`情绪或悬念延迟`；
- `目标人群窄化`、`反差对照`、`演示/案例证据`。

### 禁止的弱输出

- “社群运营、私域增长、亲子、本地生活、AI”这种没有角度和句式的词簇；
- 把标题中的数字、地点或标签直接当成市场趋势；
- 把列表可见标题改写成正文结论，或把一次样本说成已验证模式；
- 只给“可以做一个关于 X 的选题”，不给冲突、承诺、表达方式和改写模板。
- 把 `business_lens` 误写成标题关键词清单，导致所有标题都重复“门店、社群、客户、经营者”。
- 先生成门店社群结论，再从采集内容中选择性寻找支持它的链接。
- 把同一内容在点赞和收藏中的两条列表关系当成两条独立内容证据。

如果本次没有新内容，仍可生成机制复盘，但不得重复向 Base 创建同一方向。

## 飞书候选选题库写入

当且仅当 `feishu_write=true`、`feishu_target` 已提供且本次运行通过写入前确认，才执行这一段。写入目标必须在运行时解析并以真实结构为准：

- `feishu_target.base_url`：目标 Base URL；跨设备必须显式提供。
- `feishu_target.table_name` 或 `table_id`：目标数据表；不能从当前设备的历史配置猜测。
- `feishu_target.view_name` 或 `view_id`：可选，仅用于用户可见入口和回读。
- `feishu_target.field_map`：可选概念字段映射；未提供时先读取字段结构，按字段名称/描述匹配，匹配不唯一就停止。
- 待定状态字段及选项必须在目标表真实存在；不自动写入历史批次或编号。

候选方向到字段的映射：

| 字段 | 写入规则 |
| --- | --- |
| `核心选题` | 注意力优先的建议标题；作为业务去重的首要可读键，不写成抽象主题词，也不要求重复行业词 |
| `人工决定` | 固定为已有选项 `待定` |
| `来源状态` | `平台点赞/收藏增量观察；待人工复核；run_id=<...>` |
| `经营矛盾` | 目标门店/社群的具体问题 |
| `解法方向` | 尚未展开为素材的内容解法 |
| `可迁移本质` | 从平台内容抽象出的可验证机制 |
| `目标行业` | 由 `business_lens` 和人工目标决定；默认可写 `实体门店/社群运营`，但不得据此改写标题证据 |
| `热点入口` | 平台、列表类型、证据标题和内容链接的简要集合 |
| `来源文档` | 本次报告的相对路径 |
| `方向状态` | `待人工复核，未进入B` |
| `候选编号`、`批次`、`决策日期`、`修改意见` | 保持空值，不替用户编号、分批或做决策 |

写入前必须：

1. 用 `feishu_target.base_url` 解析真实 Base Token、表 ID、视图 ID，并确认字段类型和待定选项；不得使用技能包内的固定 ID。
2. 以规范化 `direction_key`（平台无关的核心选题文本加机制摘要）查询本地 `feishu-sync.jsonl`，再用 `核心选题` 精确匹配 Base 现有记录；命中则跳过创建，不能把“upsert”误当成自动查重。
3. 先输出待写入预览：候选数量、每条核心选题、证据数量、目标表、`人工决定=待定`、预计新增/跳过数量。用户明确确认后才调用记录创建命令。
4. 写入后读取返回的 `record_id`/创建数量，再回读关键字段；把 `direction_key → record_id` 追加到本地同步日志。
5. 失败时不重复提交不确定请求；记录请求摘要、返回错误和是否已知写入成功，待人工复核。

此处的“进入待定池”只表示写入 `人工决定=待定` 的候选记录，不代表保留、入选、进入 B 测试或自动发布。

## 持久化格式

项目目录约定为 `数据/平台互动快照/`：

- `snapshots/YYYY-MM-DD/<run_id>.jsonl`：本次原始观测，追加写入，不覆盖旧记录。
- `history.jsonl`：规范化后的历史观测，用于跨运行去重。
- `diffs/YYYY-MM-DD/<run_id>.json`：新增、重复、疑似移出和覆盖状态。
- `deep-read/YYYY-MM-DD/<run_id>.jsonl`：详情/视频深读结果，与列表快照分离。
- `reports/YYYY-MM-DD/<run_id>.md`：人读主报告；先列建议选题，再列机制证据、未覆盖边界和下一步。
- `analysis/YYYY-MM-DD/<run_id>.jsonl`：逐条内容机制卡；只保存标题层特征和证据边界，不把规则匹配当成趋势结论。
- `feishu-sync.jsonl`：候选方向与 Base `record_id` 的幂等同步日志。

每条列表观测至少包含：

```json
{
  "run_id": "2026-08-11T120000+08:00",
  "observed_at": "2026-08-11T12:00:00+08:00",
  "platform": "xhs",
  "list_type": "liked",
  "content_id": "6a6eda6f0000000005031dc5",
  "title": "50句社群运营秘诀，打造爆款社群！",
  "author": "彭子聊社群",
  "content_url": "https://www.xiaohongshu.com/user/profile/...",
  "source_url": "https://www.xiaohongshu.com/user/profile/...?...tab=liked",
  "content_publish_at": null,
  "is_new": true,
  "coverage_status": "incremental_boundary",
  "evidence_ref": null
}
```

`content_publish_at`、`like_at`、`favorite_at` 没有页面证据时必须为 `null`，不得用读取时间填充成操作时间。每个运行报告必须写明实际扫描屏数。

## 失败与重试

- 单个语义动作超时后最多做一次同动作重试；再次失败停止该列表，不进行无限重试。
- 记录故障类型、动作、页面 URL、当前可见状态和是否已经采到部分内容。
- 一个列表阻塞不应伪装成两个平台都完成；其他列表可继续，但报告必须分开写覆盖状态。
- 完成后优先读取当前可见页面状态并保留标签页；命令成功不是验收证据。

## 分析与人工闸门

快照只负责事实和变化检测。分析时分开写：

- **观测事实**：标题、作者、内容 ID、出现/持续/疑似移出、公开时间字段、详情正文/字幕。
- **工作推断**：建议选题、受众问题、可复用机制、跨平台重合；必须回指原始证据。
- **已验证模式**：只有经过多次、可比运行和人工复核才能升级。

不得从一次运行自动选择 TOP、自动发布或把候选标成保留。开启已确认的 Base 写入后，可以把有证据的新方向写入 `人工决定=待定`，但仍必须保留人工复核和 B 阶段闸门。

## 推荐调用样式

定时任务可以直接调用本技能并传入：

`platforms=xhs,douyin; lists=liked,favorited,favorited_videos; coverage_mode=incremental_bounded; max_screens_per_list=5; deep_read=false; feishu_write=false`

标题层规则化特征可在本地运行：

```bash
python3 技能/xhs-douyin-engagement-snapshot/scripts/analyze_titles.py \
  --input 数据/平台互动快照/latest.jsonl \
  --output 数据/平台互动快照/analysis/YYYY-MM-DD/<run_id>.jsonl
```

脚本输出是证据整理辅助，不会自动选择 TOP、判定爆款或写入飞书；候选方向仍需按上面的机制卡由人工/模型复核。

需要深读时，再追加：

`deep_read=true; deep_read_ids=<本次快照中的内容 ID>; deep_read_limit=5`

需要写入另一台设备的 Base 时，追加运行时目标（示例为占位符，不要原样提交）：

`feishu_write=true; feishu_target.base_url=<另一台设备的Base URL>; feishu_target.table_name=<目标数据表名>; feishu_target.view_name=<可选视图名>`
